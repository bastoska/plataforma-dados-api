FROM python:3.11-slim

COPY requirements.txt .

RUN pip install -r requirements.txt

WORKDIR /workload-api

COPY app/ app/
COPY tests/ tests/
COPY pytest.ini pytest.ini

ENV PYTHONUNBUFFERED=1

CMD ["pytest"]
